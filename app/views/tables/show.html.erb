<%= display_header(NAV1[:table]) %>
<%= display_nav_tables %>



<script type="text/javascript">
    var validateRoot = "http://" + document.location.host;
    function typeSelect(n) {
        var uri = validateRoot + "/tables/select_type?type=" + n;
        var xmlHttp = null;
        if (window.XMLHttpRequest) {// code for IE7, Firefox, Opera, etc.
            xmlHttp = new XMLHttpRequest();
        }
        else if (window.ActiveXObject) {// code for IE6, IE5
            xmlHttp = new ActiveXObject("Microsoft.XMLHTTP");
        }
        if (xmlHttp != null) {
            xmlHttp.open("GET", uri, false);
            xmlHttp.send(null);
            return true;
        }
        else {
            alert("Your browser does not support XMLHTTP.");
            return false
        }
    }

</script>

<style type="text/css">
    * {
        margin: 0;
        padding: 0
    }

    ul, li {
        list-style: none
    }

    .box {
        width: 1000px;
        height: 500px;
        border: 1px solid #ccc;
        margin: 10px;
        font-size: 12px;
        font-family: Verdana, Arial, Helvetica, sans-serif
    }

    .tagMenu {
        height: 28px;
        line-height: 28px;
        background: #efefef;
        position: relative;
        border-bottom: 1px solid #999
    }

    .tagMenu h2 {
        font-size: 12px;
        padding-left: 10px;
    }

    .tagMenu ul {
        position: absolute;
        left: 100px;
        bottom: -1px;
        height: 26px;
    }

    ul.menu li {
        float: left;
        margin-bottom: 1px;
        line-height: 16px;
        height: 14px;
        margin: 5px 0 0 -1px;
        border-left: 1px solid #999;
        text-align: center;
        padding: 0 12px;
        cursor: pointer
    }

    ul.menu li.current {
        border: 1px solid #999;
        border-bottom: none;
        background: #fff;
        height: 25px;
        line-height: 26px;
        margin: 0
    }

    .content {
        padding: 10px
    }

    img {
        border: 1px solid lightgrey
    }
</style>


<script type="text/javascript">

    function return_index() {
        var root_url = "http://"+document.location.host ;
        self.location = root_url + "/tables";
    }

</script>


<div class="title-main">
  <div class="title-left">
    餐桌详情
  </div>
  <div class="title-right">
    <button class="btn btn-large btn-navbar" type="submit" onclick="return_index()">返回</button>
    <%= link_to '修改', '/tables/#{@table.id}/edit', :method => :get, :class => 'btn btn-large btn-primary' %>
  </div>
</div>
<div class="main-container">
  <div
  <% if flash[:notice] %> class="alert"
  <% end %>>
    <%= flash[:notice] %>
  </div>
  <div style="clear: both;margin-top: 50px;">
    <table id="show_coupon" class="table  table-condensed" style="table-layout:fixed;width: 500px; float:left;">
      <tr>
        <td width="100">桌号:</td>
        <td><%= @table.number %></td>
      </tr>
      <tr>
        <td width="100">桌名:</td>
        <td><%= @table.name %></td>
      </tr>
      <tr>
        <td width="100" align="center">适合人数:</td>
        <td width="50">
          <div id="div_type">
            <%= @table.size %>
          </div>
        </td>
      </tr>
      <tr>
        <td width="100">状态:</td>
        <td>
          <% if @table.status == 0 %>
              空闲
          <% elsif @table.status == 1 %>
              占用
          <% elsif @table.status == 2 %>
              预定
          <% else %>
              其他
          <% end %>
        </td>
      </tr>

    </table>


  </div>

</div>