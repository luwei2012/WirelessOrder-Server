<%= form_for(@dish_style) do |f| %>
  <% if @dish_style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dish_style.errors.count, 'error') %> prohibited this dish from being saved:</h2>

      <ul>
      <% @dish_style.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
